# Avoid npm arborist peer-resolution crashes on Node 22 / npm 10
# (Cannot read properties of null reading 'matches' / 'edgesOut'),
# commonly tripped by optional peers around Next/SWC + CVA in this monorepo.
legacy-peer-deps=true
